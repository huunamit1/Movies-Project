const Options = ({ children }) => {
    return (
        <div className="z-10 absolute left-0 top-full w-full bg-white shadow-md rounded-lg">
            {children}
        </div>
    );
};

export default Options;
